= Security Architecture
:toc: left
:toclevels: 3
:toc-title: Table of Contents
:sectnums:
:source-highlighter: highlight.js

link:../../README.adoc[Back to Project Documentation]

== Overview
[.requirement]
_See Requirement: link:../Requirements.adoc#NIFI-AUTH-8[NIFI-AUTH-8: Security Requirements]_

This document describes the security architecture for the NiFi Extensions project, covering authentication and authorization across all three service layers, algorithm restrictions, SSRF protection, and multi-issuer security considerations.

== Authentication Layers

This section describes the end-to-end authentication and authorization architecture across all three service layers: the NiFi Web UI servlets, the GatewayProxyServlet, and the REST API Gateway's embedded Jetty server.

== Authentication Layers

Requests flow through up to three authentication layers depending on the target endpoint.

=== Request Flow Diagram

[source]
----
                         ┌─────────────────────────────────────────────────────────┐
                         │                    NiFi Web Container                   │
                         │                      (Layer 1)                          │
                         │  ┌───────────────────────────────────────────────────┐  │
                         │  │              Custom UI WAR (Layer 2)              │  │
                         │  │                                                   │  │
  NiFi UI                │  │  SecurityHeadersFilter ──► all responses          │  │
  (iframe)  ─── HTTPS ──►│  │         │                                         │  │
                         │  │  ProcessorIdValidationFilter                      │  │
                         │  │         │  (X-Processor-Id UUID check)            │  │
                         │  │         │                                         │  │
                         │  │         ├──► JwtVerificationServlet               │  │
                         │  │         │       └── ComponentConfigReader ──┐     │  │
                         │  │         │            (per-component auth)   │     │  │
                         │  │         │                                   │     │  │
                         │  │         ├──► JwksValidationServlet          │     │  │
                         │  │         │       (stateless, no config)      │     │  │
                         │  │         │                                   │     │  │
                         │  │         └──► GatewayProxyServlet ───────────┤     │  │
                         │  │                 │  (per-component auth)     │     │  │
                         │  │                 │  /config: local ─────────►│     │  │
                         │  │                 │  /metrics: proxy ──┐     │     │  │
                         │  │                 │  /test: proxy      │     │     │  │
                         │  │                 │  (SSRF: localhost) │     │     │  │
                         │  └─────────────────┼────────────────────┼─────┼─────┘  │
                         └────────────────────┼────────────────────┼─────┼────────┘
                                              │  HTTP (localhost)   │     │
                                              │  (loopback bypass)  │     │
                                              │                    ▼     │
                         ┌──────────────────────────────┐               │
                         │  Gateway Embedded Jetty      │◄── reads ─────┘
                         │        (Layer 3)             │  port, config
                         │                              │
                         │  GatewayRequestHandler       │
                         │  (command-pattern dispatch)  │
                         │         │                    │
  External   ── HTTP ───►│    path lookup → handler map │
  Clients                │    auth-mode dispatch:       │
  (direct)               │    BEARER / NONE / LOCAL_ONLY│
                         │         │                    │
                         │         ├──► /health ────────┤
                         │         │  HealthEndpoint    │
                         │         │  (auth: local-only)│
                         │         │                    │
                         │         ├──► /metrics ───────┤
                         │         │  MetricsEndpoint   │
                         │         │  (auth: local-only)│
                         │         │                    │
                         │         └──► /api/** ────────┤
                         │           ApiRouteHandler    │
                         │           (auth: per-route)  │
                         │           Role + scope check │
                         │           cui-http security  │
                         │                              │
                         └──────────────────────────────┘
----

Two distinct entry points reach the gateway:

* **NiFi UI path** (top): Browser -> NiFi auth -> ProcessorIdValidationFilter -> GatewayProxyServlet (serves `/config` locally; proxies `/metrics` and `/test` to gateway)
* **Direct path** (left): External client -> Gateway Jetty (single-layer: auth-mode dispatch per endpoint -- BEARER, NONE, or LOCAL_ONLY)

=== Layer 1: NiFi Container Authentication

All Custom UI endpoints run inside NiFi's web container and are served within an authenticated iframe session. Unauthenticated users cannot reach any Custom UI servlet. This is enforced by NiFi itself -- no custom code required.

=== Layer 2: Custom UI Servlet Filters and Authorization

Requests that reach the Custom UI pass through two filters and then per-servlet authorization:

[cols="1,3"]
|===
|Component |Purpose

|`SecurityHeadersFilter`
|Adds defense-in-depth response headers (`X-Content-Type-Options`, `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy`) to all responses.

|`ProcessorIdValidationFilter`
|Validates the `X-Processor-Id` (or fallback `X-Component-Id`) header as a well-formed UUID. Applied to `/nifi-api/processors/jwt/*`.

|Per-component authorization
|Servlets that read processor configuration (via `ComponentConfigReader`) trigger `NiFiWebConfigurationContext.getComponentDetails()`, which enforces NiFi's per-component access policy using the HTTP request's security principal.
|===

=== Layer 3: Gateway Embedded Jetty

The REST API Gateway processor runs its own embedded Jetty server on a configurable port. All endpoints use a unified command-pattern dispatcher with configurable `auth-mode`:

* **`BEARER`** (default for data routes) -- Full JWT Bearer authentication with role/scope authorization via `cui-http` security pipelines.
* **`LOCAL_ONLY`** (default for management endpoints) -- Loopback requests bypass authentication; remote requests require JWT Bearer auth.
* **`NONE`** -- No authentication required (anonymous access).

== Endpoint Authorization Matrix

=== Custom UI Endpoints (Layer 2)

[cols="3,1,1,1,2"]
|===
|Endpoint |Filter |Per-Component Auth |HTTP Methods |Notes

|`/nifi-api/processors/jwt/verify-token`
|ProcessorIdValidationFilter
|Yes (ComponentConfigReader)
|POST
|Reads processor config for token validation

|`/nifi-api/processors/jwt/gateway/config`
|ProcessorIdValidationFilter
|Yes (ComponentConfigReader)
|GET
|Serves config locally from processor properties

|`/nifi-api/processors/jwt/gateway/metrics`
|ProcessorIdValidationFilter
|Yes (ComponentConfigReader)
|GET
|Proxies to gateway `/metrics`

|`/nifi-api/processors/jwt/gateway/test`
|ProcessorIdValidationFilter
|Yes (ComponentConfigReader)
|POST
|Proxies test requests to gateway + SSRF protection

|`/nifi-api/processors/jwt/validate-jwks-url`
|ProcessorIdValidationFilter
|No
|POST
|Stateless validation -- no processor config access

|`/nifi-api/processors/jwt/validate-jwks-file`
|ProcessorIdValidationFilter
|No
|POST
|Stateless validation -- no processor config access

|`/nifi-api/processors/jwt/validate-jwks-content`
|ProcessorIdValidationFilter
|No
|POST
|Stateless validation -- no processor config access
|===

Endpoints without per-component authorization are still protected by NiFi container auth (Layer 1) and the ProcessorIdValidationFilter UUID check.

=== Gateway Embedded Jetty Endpoints (Layer 3)

[cols="3,1,1,1,2"]
|===
|Endpoint |Auth Mode |Authorization |HTTP Methods |Notes

|`/health` (built-in)
|`local-only` (configurable)
|None
|GET
|Returns `{"status":"UP","timestamp":"..."}`. Configurable via `rest.gateway.management.health.*` properties.

|`/metrics` (built-in)
|`local-only` (configurable)
|None
|GET
|Prometheus/JSON metrics from security event counters. Configurable via `rest.gateway.management.metrics.*` properties.

|Data routes (`/api/**`)
|`bearer` (configurable per-route)
|Role + scope validation
|Per-route config
|Full `cui-http` security pipeline (input sanitization, rate limiting via queue backpressure). Auth mode configurable via `restapi.<name>.auth-mode`.
|===

[[management-endpoint-security]]
== Management Endpoint Security

=== Auth-Mode Dispatch

Built-in management endpoints (`/health`, `/metrics`) use configurable `auth-mode` (default: `local-only`):

* **`local-only`** (default): Loopback requests (127.0.0.1, ::1) bypass authentication. Remote requests require JWT Bearer authentication. This allows localhost tools (monitoring agents, Docker health checks) to access endpoints without credentials while protecting network-exposed deployments.
* **`bearer`**: All requests require JWT Bearer authentication, same as data routes.
* **`none`**: No authentication required. Use with caution -- only appropriate when the endpoint is network-isolated.

Management endpoint auth-mode is configured via static NiFi properties:

* `rest.gateway.management.health.auth-mode` (default: `local-only`)
* `rest.gateway.management.metrics.auth-mode` (default: `local-only`)

=== Network Binding

The embedded Jetty server binds to the address specified by `rest.gateway.listening.host`:

* **Default (`0.0.0.0`)**: Listens on all interfaces. The `/metrics` management endpoint is network-accessible.
* **`127.0.0.1`**: Restricts access to localhost only. Recommended when the GatewayProxyServlet handles all external access.

=== GatewayProxyServlet as Secure Proxy

The `GatewayProxyServlet` provides authenticated access to management data:

1. Authenticated NiFi session required (Layer 1)
2. `ProcessorIdValidationFilter` validates processor UUID (Layer 2)
3. `ComponentConfigReader` enforces per-component NiFi authorization
4. `/config` is served locally from processor properties (no gateway round-trip)
5. `/metrics` is proxied to the gateway via localhost (loopback bypass when auth-mode is `local-only`)
6. SSRF protection on test endpoint: only localhost targets allowed

This means users accessing management data through the NiFi UI get full three-layer authentication, even when the gateway's management endpoints use `local-only` auth-mode.

== Security Pipeline Comparison

[cols="2,1,1,1,1"]
|===
|Capability |Data Routes |Management Endpoints |Custom UI Servlets |GatewayProxyServlet

|NiFi session auth
|No
|No
|Yes
|Yes

|Auth-mode dispatch
|`bearer` (default)
|`local-only` (default)
|N/A
|N/A

|JWT Bearer auth
|Yes (when auth-mode=bearer)
|Configurable
|No
|No

|Loopback bypass
|No (unless auth-mode=local-only)
|Yes (default)
|No
|No

|Processor ID validation
|No
|No
|Yes (filter)
|Yes (filter)

|Per-component NiFi authorization
|No
|No
|Partial (verify-token, gateway)
|Yes

|`cui-http` input sanitization
|Yes
|Yes (shared dispatcher)
|Partial (JWKS validation)
|No

|SSRF protection
|N/A
|N/A
|JWKS URL/file validation
|Localhost-only targets

|===

== Implementation Classes

[cols="2,3"]
|===
|Class |Role

|link:../../nifi-cuioss-ui/src/main/java/de/cuioss/nifi/ui/servlets/SecurityHeadersFilter.java[SecurityHeadersFilter]
|Defense-in-depth response headers on all Custom UI responses

|link:../../nifi-cuioss-ui/src/main/java/de/cuioss/nifi/ui/servlets/ProcessorIdValidationFilter.java[ProcessorIdValidationFilter]
|UUID validation for `X-Processor-Id` / `X-Component-Id` headers

|link:../../nifi-cuioss-ui/src/main/java/de/cuioss/nifi/ui/util/ComponentConfigReader.java[ComponentConfigReader]
|Per-component authorization via `NiFiWebConfigurationContext`

|link:../../nifi-cuioss-ui/src/main/java/de/cuioss/nifi/ui/servlets/GatewayProxyServlet.java[GatewayProxyServlet]
|Serves `/config` locally from processor properties; proxies `/metrics` to gateway; test endpoint with SSRF protection

|link:../../nifi-cuioss-rest-processors/src/main/java/de/cuioss/nifi/rest/handler/GatewayRequestHandler.java[GatewayRequestHandler]
|Thin command-pattern dispatcher -- path lookup, auth-mode dispatch, then delegates to `EndpointHandler`

|link:../../nifi-cuioss-rest-processors/src/main/java/de/cuioss/nifi/rest/handler/EndpointHandler.java[EndpointHandler]
|Common interface for all endpoint types (name, path, methods, authMode, enabled)

|link:../../nifi-cuioss-rest-processors/src/main/java/de/cuioss/nifi/rest/handler/HealthEndpointHandler.java[HealthEndpointHandler]
|Built-in `/health` endpoint -- returns `{"status":"UP","timestamp":"..."}`

|link:../../nifi-cuioss-rest-processors/src/main/java/de/cuioss/nifi/rest/handler/MetricsEndpointHandler.java[MetricsEndpointHandler]
|Built-in `/metrics` endpoint -- Prometheus/JSON metrics from security event counters

|link:../../nifi-cuioss-rest-processors/src/main/java/de/cuioss/nifi/rest/handler/ApiRouteHandler.java[ApiRouteHandler]
|User-configured route handler -- body validation, schema check, FlowFile enqueue

|link:../../nifi-cuioss-rest-processors/src/main/java/de/cuioss/nifi/rest/config/AuthMode.java[AuthMode]
|Enum: `BEARER`, `NONE`, `LOCAL_ONLY` -- configurable per endpoint

|link:../../nifi-cuioss-rest-processors/src/main/java/de/cuioss/nifi/rest/server/JettyServerManager.java[JettyServerManager]
|Embedded Jetty lifecycle with configurable host binding
|===

== Algorithm Restrictions

The processor restricts token algorithms to those considered secure:

[cols="1,3"]
|===
|Algorithm |Security Status

|RS256, RS384, RS512
|Secure - RSA with SHA-2

|ES256, ES384, ES512
|Secure - ECDSA with SHA-2

|PS256, PS384, PS512
|Secure - RSA PSS with SHA-2

|HS256, HS384, HS512
|**Not recommended** - Requires shared secret

|none
|**Blocked** - "none" algorithm is never allowed
|===

== SSRF Protection

By default, JWKS URLs that resolve to private, loopback, link-local, or any-local addresses are blocked to prevent Server-Side Request Forgery (SSRF) attacks. This protection can be disabled when the Identity Provider (IdP) runs on an internal network.

[cols="1,3"]
|===
|Property |Description

|`jwt.validation.jwks.allow.private.network.addresses`
|When set to `true` on the `StandardJwtIssuerConfigService` controller service, allows JWKS URLs that resolve to private/loopback network addresses. Default: `false`.
|===

**Use case:** Enterprise deployments where Keycloak or another IdP runs on a site-local address (e.g., `10.x.x.x`, `172.16.x.x`, `192.168.x.x`).

WARNING: Only enable this setting in trusted network environments where the NiFi instance and IdP are on the same private network. Disabling SSRF protection in untrusted environments exposes the system to internal network scanning attacks.

==== Security Model for Processor ID Header

The `X-Processor-Id` header used in JWKS validation requests is a *configuration lookup key*, not a security credential or trust token. It cannot be used to bypass SSRF protection:

1. **Lookup only**: The header value is passed to `NiFiWebConfigurationContext` to read the referenced processor's existing, admin-configured properties.
2. **No privilege escalation**: The `allowPrivateAddresses` flag is read from a controller service that the administrator has explicitly configured.
3. **Fail-secure defaults**: If the configuration context is unavailable, the header is missing, or any exception occurs during property resolution, the method returns `false` (private addresses blocked).
4. **NiFi session authentication**: The Custom UI servlet endpoints are only reachable within NiFi's authenticated web container (iframe).

== Multi-Issuer Security

The multi-issuer support introduces additional security considerations:

1. **Issuer Isolation**: Each issuer is validated independently
2. **Key Separation**: Each issuer has its own key set
3. **Dynamic Configuration**: Issuers can be enabled/disabled without redeployment
4. **Per-Issuer Audience**: Each issuer can have specific audience requirements

== Implementation Classes (Security)

[cols="2,3"]
|===
|Class |Role

|link:../../nifi-cuioss-ui/src/main/java/de/cuioss/nifi/ui/servlets/JwksValidationServlet.java[JwksValidationServlet]
|JWKS URL/file/content validation with SSRF and path traversal protection

|link:../../nifi-cuioss-ui/src/main/java/de/cuioss/nifi/ui/util/ComponentConfigReader.java[ComponentConfigReader]
|Secure processor configuration retrieval via NiFi REST API

|link:../../nifi-cuioss-ui/src/main/java/de/cuioss/nifi/ui/util/TokenMasking.java[TokenMasking]
|Sensitive token masking for log output
|===

== See Also

* link:jwt-rest-api.adoc[JWT REST API] -- Custom UI endpoint details and request/response formats
* link:token-validation.adoc[Token Validation]
* link:configuration.adoc[Configuration]
* link:error-handling.adoc[Error Handling]
* link:technical-components.adoc[Technical Components]
* link:../Requirements.adoc#NIFI-AUTH-8[NIFI-AUTH-8: Security Requirements]
